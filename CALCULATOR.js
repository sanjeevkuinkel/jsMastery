//? let userOperation = prompt("Enter the mathematical operation");
//?The prompt() function is part of the browser environment and not supported directly in Node.js, which is the runtime environment typically used with VS Code. When you run JavaScript in VS Code using Node.js, you won’t have access to browser-specific features like prompt(), alert(), or document.
